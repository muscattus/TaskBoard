import { MatchingFieldsDirective } from './matching-fields.directive';

describe('MatchingFieldsDirective', () => {
  it('should create an instance', () => {
    const directive = new MatchingFieldsDirective();
    expect(directive).toBeTruthy();
  });
});
